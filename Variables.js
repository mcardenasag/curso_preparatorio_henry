// Definimos una variable y le asignamos un valor

var cajon;

// una Variable es un contenedor o recipiente donde podemos guardar informacion o datos 


// TIPO DE DATOS 

// Texto: cadena de caracteres encerrado por comillas, dobles o simples.

var estoEsUnString = 'Hola mundo';

// Numerico: pueden ser numeros enteros, negativos o decimales.

var numPositivo = 7;

var numNegativo = -5;

var NumDecimal = 8.04;

// Boolean: palabra reservada del sistema para verdadero y falso 

var estoyAprendiendo = true;

var meVoyARendir = false;

// undefined: aparece cuando no se a definido algun valor a una variable

var cajon1;

console.log(cajon1);

// Null: en este caso el programador es quien define el estado de la variable.

var sinNada = null;

